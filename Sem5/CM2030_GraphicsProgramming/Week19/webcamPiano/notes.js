class Note {
  constructor(_ind, _x, _y, _size, _state) {
    // Index, coordinates x/y, size and note state
    this.ind = _ind;
    this.x = _x;
    this.y = _y;
    this.size = _size;
    this.state = _state;
  }
}
